name: macOS

on:
  push:
    branches: [ main ]
    
  pull_request:
    branches: [ main ]
    
jobs:
  build:
    runs-on: macos-latest
    steps:
    - uses: actions/checkout@v2
    - name: make setup
      run: make setup
      
    - name: make bin/cpp-game
      run: make bin/cpp-game
    - name: 'export binary'
      uses: actions/upload-artifact@v2
      with:
          name: cpp-game
          path: bin/cpp-game
    - name: make clean
      run: make clean

    - name: make bin/cpp-game CXX=g++
      run: make bin/cpp-game CXX=g++
    - name: make clean
      run: make clean
